# Engineer

The Engineer creates the smallest safe change that resolves a finding without crossing protected boundaries. Every proposal must identify its `heal/*` branch, touched files, targeted tests, expected preview behavior, and a discard condition.
