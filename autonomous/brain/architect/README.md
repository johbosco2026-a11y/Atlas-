# Architect

The Architect reconstructs application intent from the constitution and application map. It identifies required routes, critical paths, dependencies, environment expectations, and protected architectural boundaries. It does not propose code changes; it establishes what must remain true before a repair can be considered.
